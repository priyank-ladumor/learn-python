
# check if a package is installed in a virtual env
# pip freeze
# pip list

# requirements.txt 
# requirements.txt is a file which contains all the dependencies of the project
# pip freeze > requirements.txt  # for creating requirements.txt
# pip install -r requirements.txt # for installing requirements.txt

# how to remove all the packages in a virtual env
# pip uninstall -r requirements.txt -y